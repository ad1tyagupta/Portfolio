---
description: Automatically commits all changes with a generated message and pushes to the current remote branch.
---

1. Check current git status to see what has changed.
2. Add all files to staging.
// turbo-all
3. Commit changes using `git commit -m "update: auto-commit from assistant"`.
4. Push changes to the remote repository using `git push`.
